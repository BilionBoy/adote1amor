<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta name="google-site-verification" content="MPjwSEE8IUAz3c003P7aU0CMK9J65esEfCfi1ADStTY" />
    <title><%= content_for?(:title) ? yield(:title) : "Estude com Evelyn" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'Adote 1 Amor - Pets e Animais Perdidos e para Adoção em Porto Velho.' %>">
    <meta name="robots" content="index, follow">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <!-- Open Graph -->
    <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) : 'Adote 1 Amor' %>">
    <meta property="og:description" content="<%= content_for?(:description) ? yield(:description) : 'Encontre seu pet perdido e adote 1 pet!' %>">
    <meta property="og:type" content="website">
    <link rel="canonical" href="<%= request.original_url %>">
    <!-- Metadados e CSS -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "landing_application", "data-turbo-track": "reload" %>
    <!-- Fontes e ícones -->
  </head>
  <body>
    <%= yield %>
  </body>
</html>
<div class="container">
  <%= render "shared/notice" %>
  <div class="card border-0 shadow shadow-sm">
    <div class="card-header border-0 d-flex justify-content-between">
      <span class="fs-4">Visualizar A Sexo Animal</span>
    </div>
    <div class="card-body">
      <div id="<%= dom_id @a_sexo_animal %>">
          <p>
            <strong>Descricao:</strong>
            <%= @a_sexo_animal.descricao %>
          </p>
          <p>
            <strong>Sigla:</strong>
            <%= @a_sexo_animal.sigla %>
          </p>
      </div>
    </div>
    <div class="card-footer border-0 d-flex justify-content-between">
      <div>
        <%= link_to edit_a_sexo_animal_path(@a_sexo_animal), class: "btn btn-primary" do %>
        <i class="bi bi-pencil-square"></i> Editar
        <% end %>
        <%= link_to "Voltar", a_sexo_animals_path, class: "btn btn-secondary" %>
      </div>
      <%= button_to "Deletar", @a_sexo_animal, method: :delete, class: "btn btn-outline-danger border-0" %>
    </div>
  </div>
</div>
